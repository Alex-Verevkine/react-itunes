import React from "react";
import classes from "./Spinner.scss";

/**
 * @desc Loading Spinner View.
 */
const spinner = () => <div className={classes.Spinner}>Loading...</div>;

export default spinner;
